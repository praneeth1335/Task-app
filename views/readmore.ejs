<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Task Details</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body
    class="bg-zinc-900 text-white flex flex-col items-center justify-center min-h-screen p-5"
  >
    <div
      class="task-details w-96 bg-zinc-800 p-6 rounded-lg shadow-lg text-center"
    >
      <h1 class="text-3xl font-semibold tracking-tight text-white mb-3">
        <%= task.title %>
      </h1>
      <h3 class="text-lg text-zinc-300 p-4 bg-zinc-700 rounded-lg shadow-inner">
        <%= task.description %>
      </h3>
      <p class="text-sm text-zinc-400 mt-3">
        Created: <%= task.createdAt.toLocaleString() %>
      </p>
      <p class="text-sm text-zinc-400 mt-1">
        Last Updated: <%= task.updatedAt.toLocaleString() %>
      </p>
      <a
        href="/"
        class="mt-5 inline-block text-blue-500 hover:text-blue-400 transition"
      >
        ⬅ Back to Home
      </a>
    </div>
  </body>
</html>
