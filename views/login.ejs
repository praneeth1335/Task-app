<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Login | Task Manager</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
      .auth-container {
        background-color: rgba(39, 39, 42, 0.9);
        backdrop-filter: blur(10px);
      }
      .input-field:focus {
        box-shadow: 0 0 0 2px rgba(59, 130, 246, 0.5);
      }
    </style>
  </head>
  <body class="bg-zinc-900 min-h-screen flex items-center justify-center p-4">
    <div
      class="auth-container max-w-md w-full p-8 rounded-xl shadow-2xl border border-zinc-700"
    >
      <div class="text-center mb-8">
        <h1 class="text-3xl font-bold text-white mb-2">Welcome Back</h1>
        <p class="text-zinc-400">Log in to manage your tasks</p>
      </div>

      <!-- Show Error Message -->
      <% if (message) { %>
      <div
        class="bg-red-500 text-white px-4 py-2 rounded mb-4 text-sm font-medium text-center"
      >
        <%= message %>
      </div>
      <% } %>

      <!-- Show Success Message -->
      <% if (success_msg) { %>
      <div
        class="bg-green-500 text-white px-4 py-2 rounded mb-4 text-sm font-medium text-center"
      >
        <%= success_msg %>
      </div>
      <% } %>

      <form action="/login" method="POST" class="space-y-6">
        <div>
          <label
            for="username"
            class="block text-sm font-medium text-zinc-300 mb-1"
            >Username</label
          >
          <input
            type="text"
            id="username"
            name="username"
            required
            class="input-field w-full px-4 py-3 bg-zinc-800 border border-zinc-700 rounded-lg text-white placeholder-zinc-500 focus:outline-none"
            placeholder="Enter your username"
          />
        </div>

        <div>
          <label
            for="password"
            class="block text-sm font-medium text-zinc-300 mb-1"
            >Password</label
          >
          <input
            type="password"
            id="password"
            name="password"
            required
            class="input-field w-full px-4 py-3 bg-zinc-800 border border-zinc-700 rounded-lg text-white placeholder-zinc-500 focus:outline-none"
            placeholder="Enter your password"
          />
        </div>

        <button
          type="submit"
          class="w-full bg-blue-600 hover:bg-blue-700 text-white font-medium py-3 px-4 rounded-lg transition duration-200"
        >
          Log In
        </button>
      </form>

      <div class="mt-6 text-center">
        <p class="text-zinc-400">
          Don't have an account?
          <a
            href="/register"
            class="text-blue-500 hover:text-blue-400 font-medium"
            >Sign up</a
          >
        </p>
      </div>
    </div>
  </body>
</html>
