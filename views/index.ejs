<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Task Manager</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-900 p-10">
      <div class="flex justify-between items-center mb-8">
        <h1 class="text-2xl font-bold text-white">
          Welcome, <%= user.username %>
        </h1>
        <form action="/logout" method="GET">
          <button
            class="text-red-500 hover:text-red-700 px-4 py-2 rounded-md bg-zinc-800"
            type="submit"
          >
            Logout
          </button>
        </form>
      </div>

      <div class="top-section flex gap-5 justify-center">
        <div class="form bg-zinc-800 p-5 rounded-lg text-white">
          <form action="/tasks" method="POST">
            <input
              class="block w-80 outline-none rounded-md px-5 py-2 bg-zinc-700"
              type="text"
              name="title"
              placeholder="Title goes here ..."
              required
            />
            <textarea
              class="block w-80 outline-none rounded-md mt-2 px-5 py-2 resize-none bg-zinc-700"
              placeholder="Task details go here ..."
              name="description"
              required
            ></textarea>
            <input
              class="mt-2 w-80 justify-center items-center bg-blue-600 px-5 py-2 rounded-md cursor-pointer"
              type="submit"
              value="Create Task"
            />
          </form>
        </div>

        <div class="portfolio w-72 bg-zinc-800 p-5 rounded-lg">
          <h2 class="text-2xl font-bold underline-middle">B S Praneeth</h2>
          <p class="text-zinc-400 mt-1">Developer</p>
          <div class="social-links mt-4">
            <a href="#" target="_blank" class="text-blue-500 block">LinkedIn</a>
            <a href="#" target="_blank" class="text-pink-500 block mt-2"
              >Instagram</a
            >
          </div>
        </div>
      </div>

      <div class="tasks flex gap-5 flex-wrap p-10 w-full mt-10">
        <% if (tasks.length > 0) { %> <% tasks.forEach((task) => { %>
        <div
          class="task w-60 h-60 flex flex-col justify-center items-center bg-zinc-800 rounded-lg transform transition-all duration-300 hover:scale-105 hover:shadow-lg hover:bg-zinc-700"
        >
          <h1 class="text-3xl font-semibold tracking-tight text-center">
            <%= task.title %>
          </h1>
          <p class="taskDesc mt-2 text-sm text-center px-4">
            <%= task.description %>
          </p>
          <div class="mt-4 flex space-x-4">
            <a
              class="text-blue-500 inline-block mt-2"
              href="/tasks/<%= task._id %>/edit"
              >Edit</a
            >
            <a
              class="text-red-500 inline-block mt-2"
              href="/tasks/<%= task._id %>/delete"
              >Delete</a
            >
          </div>
          <a class="text-blue-500 block mt-3" href="/readmore/<%= task._id %>/"
            >Read More...</a
          >
        </div>
        <% }); %> <% } else { %>
        <h3 class="text-zinc-600 text-center w-full">
          No tasks yet. Create your first task!
        </h3>
        <% } %>
      </div>
    </div>
    <script src="/js/script.js"></script>
  </body>
</html>
